import type { Metadata } from "next"

export const metadata: Metadata = {
  title: {
    absolute: "basement.studio | 3D Scene"
  }
}

const Homepage = () => {
  return null
}

export default Homepage
